# Linux 0.01 内核项目结构梳理

## 一、项目概述

这是 **Linux 0.01** 版本的内核源码，由 Linus Torvalds 于 1991 年发布，是 Linux 操作系统的第一个公开版本。这是一个教学性质的极简内核，仅支持 i386 架构，实现了操作系统的核心功能。

### 1.1 核心特征
- **架构**: i386 (x86 32位)
- **内核大小**: 限制在 512KB 以内
- **内存管理**: 支持分页和虚拟内存
- **文件系统**: MINIX 文件系统 v1
- **进程管理**: 支持多任务调度
- **设备驱动**: 串口、硬盘、控制台、键盘

---

## 二、项目目录结构

```
linux-0.01/
├── boot/              # 引导加载程序
├── init/              # 系统初始化
├── kernel/            # 内核核心模块
├── mm/                # 内存管理
├── fs/                # 文件系统
├── lib/               # 内核库函数
├── include/           # 头文件
├── tools/             # 构建工具
└── Makefile           # 主构建脚本
```

---

## 三、各模块详细解析

### 3.1 boot/ - 引导加载程序

**功能**: 负责系统启动，从实模式切换到保护模式

#### 核心文件
- **boot.s**: BIOS 引导扇区代码
  - 被 BIOS 加载到 0x7c00
  - 移动自身到 0x90000
  - 使用 BIOS 中断加载内核系统到 0x10000
  - 切换到保护模式
  - 支持软盘类型配置（1.44Mb/1.2Mb/720kB）

- **head.s**: 保护模式初始化
  - 重新初始化保护模式表
  - 启用中断
  - 设置初始 GDT、IDT

#### 关键地址映射
```
0x00000 - 内核起始位置
0x07c00 - BIOS 加载 boot.s 的位置
0x10000 - 系统内核加载位置 (64KB)
0x90000 - boot.s 移动后的位置
```

---

### 3.2 init/ - 系统初始化

**功能**: 内核入口点和系统初始化

#### 核心文件
- **main.c**: 内核主入口
  - `main()`: 内核启动入口
  - `time_init()`: 从 CMOS 实时时钟初始化时间
  - 初始化各子系统（内存、中断、进程、设备）
  - 启动初始进程

#### 启动流程
1. 设置段寄存器
2. 初始化中断描述符表
3. 初始化内存管理
4. 初始化进程调度
5. 初始化字符设备和块设备
6. 挂载根文件系统
7. 创建进程 0（idle 进程）
8. 创建进程 1（init 进程）

---

### 3.3 kernel/ - 内核核心模块

**功能**: 实现操作系统的核心功能

#### 模块分类

##### 3.3.1 进程管理
- **sched.c**: 进程调度器
  - `schedule()`: 主调度函数，基于时间片轮转
  - 实现 `sleep_on()`, `wake_up()` 等睡眠/唤醒机制
  - Task 0 为 idle 进程
  - 支持信号处理（alarm、SIGALRM）
  - 进程优先级计算

- **fork.c**: 进程创建
  - `copy_page_tables()`: 复制页表（实现 fork）
  - 支持写时复制（Copy-On-Write）
  - 进程树管理

- **exit.c**: 进程退出
  - 资源清理
  - 子进程处理
  - 信号处理

##### 3.3.2 系统调用
- **sys.c**: 系统调用实现
  - 实现 POSIX 系统调用接口
  - 部分系统调用返回 -ENOSYS（未实现）
  - 已实现：setgid, sys_time, times, uname 等
  - 进程管理、时间相关调用

- **system_call.s**: 系统调用入口（汇编）
  - 中断 0x80 处理
  - 系统调用表分派
  - 用户态/内核态切换

##### 3.3.3 中断与异常
- **traps.c**: 陷阱和异常处理
  - 硬件异常处理（除零、页错误等）
  - 写保护异常（do_wp_page）
  - 缺页异常（do_no_page）

- **asm.s**: 底层汇编例程
  - 数学协处理器处理
  - 底层系统操作

##### 3.3.4 设备驱动
- **console.c**: 控制台驱动
  - 屏幕输出
  - 光标控制
  - 与 tty 层交互

- **keyboard.s**: 键盘驱动（汇编）
  - 键盘中断处理
  - 扫描码转换

- **serial.c**: 串口驱动
  - RS-232 串口通信
  - 中断驱动 I/O

- **rs_io.s**: 串口 I/O 汇编优化

- **hd.c**: 硬盘驱动
  - 硬盘控制器编程
  - 块设备 I/O
  - `hd_init()`: 硬盘初始化

##### 3.3.5 终端子系统
- **tty_io.c**: TTY 终端 I/O
  - 终端抽象层
  - 字符缓冲
  - 信号处理（SIGINT 等）

##### 3.3.6 内核工具
- **printk.c**: 内核打印函数
  - 类似 printf 的内核日志
  - 控制台输出

- **vsprintf.c**: 格式化字符串
  - `vsprintf()` 实现

- **panic.c**: 内核恐慌处理
  - `panic()`: 致命错误处理
  - 系统挂起

- **mktime.c**: 时间转换
  - `kernel_mktime()`: 时间戳转换

---

### 3.4 mm/ - 内存管理

**功能**: 分页内存管理和虚拟内存

#### 核心文件
- **memory.c**: 内存管理核心
  - **页分配**: `get_free_page()`, `free_page()`
  - **页表操作**: `copy_page_tables()`, `free_page_tables()`, `put_page()`
  - **写时复制**: `do_wp_page()`, `un_wp_page()`
  - **缺页处理**: `do_no_page()`
  - **内存映射**: `mem_map[]` 数组管理物理页
  - 页大小：4KB
  - 支持 4MB 页目录项

- **page.s**: 页错误处理（汇编）
  - 页错误中断处理
  - 调用 C 语言异常处理函数

#### 内存布局
```
LOW_MEM (0x100000 = 1MB): 内核和缓冲区的上界
HIGH_MEMORY: 系统物理内存上限
PAGING_MEMORY = HIGH_MEMORY - LOW_MEM: 可分页内存
```

#### 关键机制
1. **页目录**: 1024 项，每项 4MB
2. **页表**: 1024 项，每项 4KB
3. **写时复制**: fork 时共享页，写时复制
4. **按需分页**: 缺页时分配新页

---

### 3.5 fs/ - 文件系统

**功能**: MINIX 文件系统 v1 实现

#### 模块分类

##### 3.5.1 超级块管理
- **super.c**: 超级块处理
  - `do_mount()`: 挂载文件系统
  - `mount_root()`: 挂载根文件系统
  - 读取和验证超级块魔数（SUPER_MAGIC）
  - 管理 inode 位图和区块位图

##### 3.5.2 Inode 管理
- **inode.c**: Inode 操作
  - Inode 分配和释放
  - Inode 缓存管理
  - 文件属性管理

- **bitmap.c**: 位图管理
  - Inode 位图操作
  - 数据块位图操作
  - `set_bit()`: 设置位

##### 3.5.3 文件操作
- **file_dev.c**: 文件设备操作
  - 文件读写
  - 块设备文件访问

- **file_table.c**: 文件表管理
  - 系统级文件表
  - 文件引用计数

- **read_write.c**: 读写操作
  - `sys_read()`, `sys_write()`
  - 文件偏移管理

##### 3.5.4 目录与文件名
- **namei.c**: 路径名解析
  - 路径名到 inode 的转换
  - 目录项查找
  - `.` 和 `..` 处理

- **open.c**: 文件打开
  - `sys_open()`, `sys_creat()`
  - 权限检查
  - 文件标志处理

- **fcntl.c**: 文件控制
  - `sys_fcntl()`
  - 文件描述符操作

##### 3.5.5 设备文件
- **block_dev.c**: 块设备操作
  - 块设备读写
  - 缓冲区管理

- **char_dev.c**: 字符设备操作
  - 字符设备读写
  - 设备号管理

##### 3.5.6 缓冲区
- **buffer.c**: 缓冲区缓存
  - 块设备缓存
  - `bread()`: 读取缓冲块
  - `brelse()`: 释放缓冲块
  - LRU 缓存替换策略

##### 3.5.7 其他
- **exec.c**: 程序执行
  - `sys_execve()`
  - a.out 格式加载
  - 参数传递

- **pipe.c**: 管道
  - 进程间通信
  - 阻塞读写

- **truncate.c**: 文件截断
  - 文件大小调整
  - 块释放

- **stat.c**: 文件状态
  - `sys_stat()`, `sys_fstat()`
  - 文件属性查询

- **ioctl.c**: 设备控制
  - `sys_ioctl()`

- **tty_ioctl.c**: TTY 控制
  - 终端 IOCTL

---

### 3.6 lib/ - 内核库函数

**功能**: 内核态标准库实现

#### 核心文件
- **string.c**: 字符串操作
  - `strlen()`, `strcpy()`, `strncpy()`
  - `memcpy()`, `memset()`

- **ctype.c**: 字符类型判断
  - `isdigit()`, `isalpha()` 等

- **errno.c**: 错误号
  - 全局 errno 变量

- **系统调用包装器**:
  - **open.c**: `open()`
  - **close.c**: `close()`
  - **dup.c**: `dup()`
  - **wait.c**: `wait()`
  - **write.c**: `write()`
  - **execve.c**: `execve()`
  - **_exit.c**: `_exit()`
  - **setsid.c**: `setsid()`

---

### 3.7 include/ - 头文件

**功能**: 内核和系统接口定义

#### 目录结构
```
include/
├── linux/        # Linux 内核头文件
│   ├── sched.h   # 进程调度
│   ├── mm.h      # 内存管理
│   ├── fs.h      # 文件系统
│   ├── hdreg.h   # 硬盘寄存器
│   ├── tty.h     # 终端
│   ├── sys.h     # 系统调用
│   ├── kernel.h  # 内核
│   ├── head.h    # 头部定义
│   └── config.h  # 配置
├── asm/          # 架构相关头文件
│   ├── io.h      # I/O 操作
│   ├── system.h  # 系统操作
│   ├── segment.h # 段管理
│   └── memory.h  # 内存
├── sys/          # 系统头文件
│   ├── stat.h    # 文件状态
│   ├── types.h   # 类型定义
│   └── ...
├── signal.h      # 信号
├── unistd.h      # 标准 Unix 接口
├── termios.h     # 终端 I/O
├── fcntl.h       # 文件控制
├── errno.h       # 错误号
└── ...
```

---

### 3.8 tools/ - 构建工具

**功能**: 内核构建辅助工具

#### 核心文件
- **build.c**: 内核镜像构建工具
  - 合并 boot 和 system 部分
  - 生成最终 Image 文件
  - 计算系统大小

---

## 四、构建系统

### 4.1 构建流程

```
1. 编译 boot/boot.s (16位汇编)
   ↓
2. 编译 boot/head.s (32位保护模式)
   ↓
3. 编译 init/main.o
   ↓
4. 编译 kernel/, mm/, fs/, lib/
   ↓
5. 链接为 tools/system
   ↓
6. 使用 tools/build 合并为 Image
```

### 4.2 编译工具链
- **AS86/CC86/LD86**: 16位工具链（用于 boot）
- **gcc/gas/gld**: 32位工具链（用于内核）
- **CFLAGS**: 优化标志（-Wall -O -fstrength-reduce 等）

### 4.3 输出文件
- **Image**: 可启动的内核镜像
- **System.map**: 内核符号表

---

## 五、核心数据结构

### 5.1 进程结构
```c
struct task_struct {
    /* 进程状态 */
    long state;
    long counter;      /* 时间片 */
    long priority;
    
    /* 信号 */
    long signal;
    
    /* 进程标识 */
    int pid;
    int father;
    
    /* 文件系统 */
    struct m_inode * pwd;
    struct m_inode * root;
    
    /* 内存管理 */
    unsigned long start_code;
    unsigned long end_code;
    unsigned long end_data;
    unsigned long brk;
    unsigned long start_stack;
    
    /* TSS */
    struct tss_struct tss;
};
```

### 5.2 内存管理
- **页目录**: 1024 项 × 4 字节
- **页表**: 1024 项 × 4 字节
- **页大小**: 4KB
- **物理页映射**: `mem_map[]` 数组

### 5.3 文件系统
- **超级块**: `struct super_block`
- **Inode**: `struct m_inode`
- **目录项**: `struct dir_entry`
- **文件**: `struct file`

---

## 六、系统启动流程

```
BIOS 启动
    ↓
boot.s (0x7c00)
    ├─ 移动到 0x90000
    ├─ 加载 system 到 0x10000
    └─ 切换到保护模式
    ↓
head.s
    ├─ 设置 GDT、IDT
    └─ 跳转到 main()
    ↓
main()
    ├─ 初始化内存管理
    ├─ 初始化中断
    ├─ 初始化进程调度
    ├─ 初始化设备驱动
    ├─ 挂载根文件系统
    └─ 创建进程 0 和进程 1
```

---

## 七、技术特点与局限

### 7.1 技术特点
1. **极简设计**: 代码精简，易于学习
2. **完整 OS**: 包含进程、内存、文件系统等核心组件
3. **i386 保护模式**: 充分利用 32 位 CPU 特性
4. **虚拟内存**: 支持分页和写时复制
5. **多任务**: 基于时间片的抢占式调度

### 7.2 局限性
1. **仅支持 i386**: 无硬件抽象层
2. **文件系统简单**: 仅支持 MINIX FS v1
3. **无 SMP**: 不支持多处理器
4. **无网络**: 没有网络协议栈
5. **内存限制**: 内核限制在 512KB
6. **设备驱动少**: 仅有基本设备驱动

---

## 八、文件统计

| 模块 | C 文件 | 汇编文件 | 头文件 | 说明 |
|------|--------|----------|--------|------|
| boot/ | 0 | 2 | 0 | 引导程序 |
| init/ | 1 | 0 | 0 | 初始化 |
| kernel/ | 11 | 4 | 0 | 内核核心 |
| mm/ | 1 | 1 | 0 | 内存管理 |
| fs/ | 18 | 0 | 0 | 文件系统 |
| lib/ | 12 | 0 | 0 | 库函数 |
| include/ | 0 | 0 | 25+ | 头文件 |

**总计**: 约 42 个 C 文件，7 个汇编文件，25+ 个头文件

---

## 九、历史意义

Linux 0.01 是操作系统发展史上的里程碑：
- **第一个公开版本**: 1991 年 9 月发布
- **开创性**: 证明了个人可以开发完整操作系统
- **教育价值**: 代码简洁，是学习操作系统的绝佳材料
- **开源精神**: 奠定了 Linux 开源社区的基础

---

## 十、学习建议

### 10.1 阅读顺序
1. **boot/** - 了解启动过程
2. **init/main.c** - 理解系统初始化
3. **kernel/sched.c** - 学习进程调度
4. **mm/memory.c** - 掌握内存管理
5. **fs/** - 理解文件系统
6. **kernel/sys.c** - 系统调用接口

### 10.2 关键概念
- 保护模式与实模式
- 分页与虚拟内存
- 进程调度算法
- 系统调用机制
- 文件系统实现
- 中断处理

### 10.3 参考资料
- 《Linux 内核完全注释》- 赵炯
- 《Understanding the Linux Kernel》- Daniel P. Bovet
- Linux 0.01 源码注释版

---

**文档生成时间**: 2026-09-21  
**内核版本**: Linux 0.01  
**架构**: i386 (x86 32-bit)
